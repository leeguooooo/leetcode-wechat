const merge = require('lodash/merge')
const eslintConfig = require('./.eslintrc')

const WARN = 1

module.exports = merge({}, eslintConfig, {
  // 对开发友好：把开发中容易「临时」触发的规则设为警告。
  rules: {
    'no-constant-condition': WARN,
    'no-debugger': WARN,
    'no-empty': WARN,
    'no-unused-vars': WARN,
    'no-console': WARN,
  },
})
