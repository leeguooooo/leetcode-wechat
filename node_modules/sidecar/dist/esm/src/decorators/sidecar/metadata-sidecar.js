import { log, } from '../../config.js';
import { SIDECAR_SYMBOL } from './constants.js';
function updateMetadataSidecar(target, view) {
    log.verbose('Sidecar', 'updateMetadataSidecar(%s, "%s...")', target.name, JSON.stringify(view).substr(0, 20));
    // log.silly('Sidecar', 'updateMetadataSidecar(%s, %s)',
    //   target.name,
    //   JSON.stringify(view)
    // )
    // Update the parameter names
    Reflect.defineMetadata(SIDECAR_SYMBOL, view, target);
}
function getMetadataSidecar(target) {
    // Pull the array of parameter names
    const view = Reflect.getMetadata(SIDECAR_SYMBOL, target);
    return view;
}
export { getMetadataSidecar, updateMetadataSidecar, };
//# sourceMappingURL=metadata-sidecar.js.map