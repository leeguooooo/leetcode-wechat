import { log } from 'brolog';
function clean(session, script) {
    log.verbose('Sidecar', 'clean()');
    script.unload().catch(console.error);
    session.detach().catch(console.error);
}
export { clean };
//# sourceMappingURL=clean.js.map